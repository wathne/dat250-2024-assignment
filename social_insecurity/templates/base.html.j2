<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Meta -->
    <meta charset="utf-8" />
    <meta name="viewport"
          content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <meta name="description"
          content="Social Insecurity - The social network for the insecure™">
    <meta name="keywords" content="social network, security">
    <title>{{ title }} - Social Insecurity</title>
    <!-- Local files -->
    <link rel="shortcut icon" href={{ url_for('static', filename='favicon.ico') }}>
    <link rel="stylesheet" href={{ url_for('static', filename='css/general.css') }} />
    <!-- CSS from remotes -->
    <link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css"
          rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
          rel="stylesheet"
          integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
          crossorigin="anonymous">
  </head>
  <body>
    <!-- Navbar - Index page -->
    {% if title == 'Welcome' %}
      <nav class="navbar navbar-dark bg-primary fixed-top">
        <div class="container-fluid">
          <a class="navbar-brand" href="#">Social Insecurity</a>
        </div>
      </nav>
      <!-- Navbar - Logged in -->
    {% else %}
      <nav class="navbar navbar-dark bg-primary navbar-expand-lg fixed-top">
        <div class="container-fluid">
          <button class="navbar-toggler"
                  type="button"
                  data-bs-toggle="collapse"
                  data-bs-target="#navbar-toggle"
                  aria-controls="navbar-toggle"
                  aria-expanded="false"
                  aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbar-toggle">
            <a class="navbar-brand" href={{ url_for('stream', username=username) }}>Social Insecurity</a>
            <ul class="navbar-nav me-auto mb-lg-0">
              <li class="nav-item active">
                {% if title == 'Stream' or title == 'Comments' %}
                  <a class="nav-link active" href={{ url_for('stream', username=username) }}>Stream<span class="sr-only">(current)</span></a>
                {% else %}
                  <a class="nav-link" href={{ url_for('stream', username=username) }}>Stream</a>
                {% endif %}
              </li>
              <li class="nav-item">
                {% if title == 'Friends' %}
                  <a class="nav-link active" href={{ url_for('friends', username=username) }}>Friends<span class="sr-only">(current)</span></a>
                {% else %}
                  <a class="nav-link" href={{ url_for('friends', username=username) }}>Friends</a>
                {% endif %}
              </li>
              <li class="nav-item">
                {% if title == 'Profile' %}
                  <a class="nav-link active" href={{ url_for('profile', username=username) }}>Profile<span class="sr-only">(current)</span></a>
                {% else %}
                  <a class="nav-link" href={{ url_for('profile', username=username) }}>Profile</a>
                {% endif %}
              </li>
              <li class="nav-item">
                <a class="nav-link link-light" href={{ url_for('index') }} role="button">Log Out</a>
              </li>
            </ul>
          </div>
        </div>
      </nav>
    {% endif %}
    <!-- Flash alert dialog -->
    {% include "alert.html.j2" %}
    <main>
      <!-- Content from child templates -->
      {% block content %}
      {% endblock content %}
    </main>
    <!-- Javascript from remotes -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"
            integrity="sha384-IQsoLXl5PILFhosVNubq5LC7Qb9DXgDA9i+tQ8Zj3iwWAwPtgFTxbJ8NT4GN1R8p"
            crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
            integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
            crossorigin="anonymous"></script>
    <!-- Javascript from child templates -->
    {% block script %}
    {% endblock script %}
  </body>
</html>
